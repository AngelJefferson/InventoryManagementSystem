services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:80"
    environment:
      ASPNETCORE_ENVIRONMENT: "Production"
      ASPNETCORE_URLS: "http://+:80"
      ConnectionStrings__DefaultConnection: "Host=db.xiyvoqhlcoytkzggvehu.supabase.co;Database=postgres;Username=postgres;Password=Inventario9920;SSL Mode=Require;Trust Server Certificate=true"
      Jwt__Key: "InventoryManagementSuperSecretKey2024!@#$%"
      Jwt__Issuer: "InventoryManagementAPI"
      Jwt__Audience: "InventoryManagementClient"
    restart: unless-stopped
